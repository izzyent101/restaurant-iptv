<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RestaurantIptv" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Cold-start splash: backlit MMG emblem on black, visible the instant
         the app process launches, before any code runs. -->
    <style name="Theme.RestaurantIptv.Splash">
        <item name="android:windowBackground">@drawable/splash_bg</item>
    </style>
</resources>
